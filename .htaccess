######### Generated by Lwspanel #########
######### Ne pas modifier / Don't touch #########

# Redirections https dusart-music.be
RewriteEngine On
RewriteCond %{HTTP:X-Forwarded-Proto} !https
RewriteCond %{HTTPS} !on
RewriteCond %{HTTP_HOST} ^(?:www\.)?dusart-music\.be(?:.*)$ [nc]
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [redirect=301,L]

######### End Generated by Lwspanel #########

# Canonical domain: force https://www.dusart-music.be
RewriteEngine On
# If host is not www.dusart-music.be but matches the root domain, redirect to www
RewriteCond %{HTTP_HOST} !^www\.dusart-music\.be$ [NC]
RewriteCond %{HTTP_HOST} ^dusart-music\.be$ [NC]
RewriteRule ^(.*)$ https://www.dusart-music.be/$1 [L,R=301]
